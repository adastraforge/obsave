# Ad Astra Forge — Reglas del Agente para ObSave

## Identidad del Proyecto
- **Firma:** Ad Astra Forge
- **Plugin:** ObSave (`obsave`) — suite de productividad local para Obsidian
- **Stack:** TypeScript, Obsidian API, esbuild
- **Licencia:** 100% Gratuito y Open Source

## Premisas Arquitectónicas (Inviolables)
1. Sin suscripciones, llaves pagadas ocultas ni conectores bloqueados.
2. 100 % local: sin proveedores de nube, OAuth, ledger remoto ni sincronización en segundo plano.
3. Todas las operaciones (carpetas plantilla, nota rápida, nueva nota, informe) usan el sistema de archivos nativo de Obsidian.
4. El Hub lateral es el dashboard principal de productividad.

## Build Gate (Obligatorio)
Antes de sugerir o ejecutar `git commit` o `git push`:
1. Ejecutar `npm run build`.
2. Si el build falla, **detenerse** y reparar antes de continuar.
3. No proponer commits con el proyecto en estado de compilación rota.

## Release Automático de Obsidian (Obligatorio)
Cada vez que se solicite un **release** o **versión de producción**, el agente debe:
1. Ejecutar `npm run build` y verificar **exit code 0**.
2. Sincronizar la versión en `manifest.json` y `package.json`.
3. Incluir `main.js` y `manifest.json` en el repositorio (`git add -f main.js` si aplica).
4. Publicar el Release en GitHub con:
   ```bash
   gh release create <tag> main.js manifest.json --title '<titulo>' --notes '<notas>'
   ```
5. Si `gh CLI` no está disponible, crear/disparar el workflow en `.github/workflows/release.yml` mediante **tags de git**:
   ```bash
   git tag -a <tag> -m "<titulo>"
   git push origin <tag>
   ```
6. Actualizar `.cursor/context.md` y `.cursor/decision_log.md` confirmando el release oficial.

## Documentación Viva
Tras cambios estructurales (nuevos módulos, capas, flujos de captura o estrategia del Hub):
1. Actualizar `.cursor/context.md` si cambia visión, fase o premisas.
2. Actualizar `.cursor/architecture.md` si cambian capas, diagramas o contratos.
3. Registrar decisiones en `.cursor/decision_log.md` con fecha, contexto y alternativas descartadas.

## Estándares de Código
- TypeScript estricto.
- Separación de capas: UI (Hub, Ajustes, modales) → productividad local (captura, estructura, informe).
- Sin dependencias comerciales ni clientes de red.
- Comentarios solo para lógica no obvia; el código debe ser autoexplicativo.
- Seguir convenciones existentes del repo antes de introducir nuevos patrones.

## Idioma
- Documentación de proyecto y commits en español cuando aplique al contexto del equipo.
- Identificadores de código en inglés (convención TypeScript/Obsidian).
